UPDATE FOR analytics-skills
===========================

Unzip the CONTENTS of this archive over your repo root, overwriting when asked:

    C:\Agentic AI\Skills\analytics-skills\

The warehouse .duckdb file is deliberately NOT included -- your copy is already
functionally identical (44 tables, canonical headcount 3,647). Only the byte
layout differs, which is DuckDB internal ordering, not data.

Then:

    cd "C:\Agentic AI\Skills\analytics-skills"
    python check_setup.py
    git status
    git add -A
    git commit -m "Add references, workshop deck, setup checker, eval fixes"
    git push

VERIFY BEFORE RUNNING THE ABLATION
----------------------------------
    cd evals
    python run_evals.py --help

You must see --repeats and --load-all. If not, the overlay did not land.

WHAT'S NEW
----------
  references/analysis-patterns.md      five patterns, all SQL tested
  references/domain-doc-template.md    the BUILD-block artifact
  references/EXAMPLE-headcount.md      worked example, deliberately incomplete
  references/verify_sql.py             executes every SQL block in references/
  workshop/build_deck.js               generates the 16-slide deck
  check_setup.py                       catches the pip/python mismatch
  requirements.txt                     one-command setup
  HANDOFF.md                           session state for picking up later
  evals/run_evals.py                   --repeats, --load-all, per-slice loading
  evals/provenance-footer/evals.json   adds prv-04 negative test (29 total)
