UPDATE FOR analytics-skills  —  2026-08-04
==========================================

Unzip the CONTENTS over your repo root, overwriting when asked:

    C:\Agentic AI\Skills\analytics-skills\

The warehouse .duckdb is deliberately NOT included. Your copy is already
functionally identical (44 tables, canonical headcount 3,647); only DuckDB's
internal byte layout differs.

Then:

    cd "C:\Agentic AI\Skills\analytics-skills"
    python check_setup.py
    git add -A
    git commit -m "Add references, workshop deck, setup checker, parallel eval runner"
    git push

VERIFY IT LANDED
----------------
    cd evals
    python run_evals.py --help

You must see --workers, --max-turns, --repeats, and --load-all.

THEN RUN THE ABLATION  (~20 minutes, not 2.5 hours)
---------------------------------------------------
    Remove-Item results\*.json -ErrorAction SilentlyContinue
    python run_evals.py --mode baseline --repeats 3 --workers 16
    python run_evals.py --mode skills  --repeats 3 --workers 16
    python run_evals.py --compare results\baseline.json results\skills.json

Read HANDOFF.md first next session — it is the state of the whole project.
